version: '3.8'

services:
  postgres-test:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ai-ecommerce-postgres-test
    restart: unless-stopped
    environment:
      POSTGRES_DB: ai_ecommerce_test
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    networks:
      - ai-ecommerce-network

volumes:
  postgres_data:
  
networks:
  ai-ecommerce-network:
    driver: bridge