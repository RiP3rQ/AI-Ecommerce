/**
 * Maximum number of reviews to include in the summary generation.
 * This prevents the AI from being overwhelmed with too much data.
 */
export const MAX_REVIEWS_FOR_SUMMARY = 50;

/**
 * Maximum output tokens for the summary generation.
 */
export const MAX_SUMMARY_TOKENS = 8000;
